1. Requires nodejs to run
2. once you have installed run `npm i` to get all required packages
3. use `node <file>` to run specific file
4. some require arguements, figure it out on your own for now, ill update this with more info later
